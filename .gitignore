# Dependencies
node_modules
.pnp
.pnp.js

# Testing
coverage
.nyc_output

# Playwright (apps/web/e2e-ui)
apps/web/test-results
apps/web/playwright-report
apps/web/playwright/.cache
apps/web/blob-report

# Next.js
.next
.next-mobile
out

# Nuxt
.nuxt
dist

# Nest
dist
build

# Expo
.expo
.expo-shared
dist
npm-debug.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*
web-build

# Environment variables
.env
.env.local
.env.*.local
.env.production.local
# e2e testlerinin ortamı — sır içerir, her geliştirici kendi kopyasını
# oluşturur (bkz. apps/api/test/README.md).
.env.test

# IDE
.vscode
.idea
*.swp
*.swo
*~
.DS_Store

# OS
.DS_Store
.env.local
.env.*.local
Thumbs.db

# Monorepo
dist
.turbo

# Prisma
prisma/migrations

# Build
.next
.nuxt
dist
build
# TypeScript incremental derleme artığı — makineye özel, commit'lenmemeli
# (bayat bir tsbuildinfo CI'da tip kontrolünün dosya atlamasına yol açabilir).
*.tsbuildinfo

# Logs
logs
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*

# Package manager
# pnpm-lock.yaml BİLEREK ignore edilmiyor: tekrarlanabilir kurulum için
# commit'lenmeli. CI hem `pnpm install --frozen-lockfile` hem setup-node'un
# pnpm cache'i için bu dosyaya ihtiyaç duyuyor.
# Projede pnpm kullanılıyor; diğer paket yöneticilerinin lock dosyaları
# yanlışlıkla commit'lenmesin diye ignore'da kalıyor.
package-lock.json
yarn.lock
